from django.apps import AppConfig  # BU SATIR EKSİK!

class LocationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'locations'
    verbose_name = 'Bölgeler ve Lokasyonlar'